defaults:
  - base_text_semicat

in_shape:
  - ${data.k}
  - ${net.vocab_size}

net:
  hidden_size: 768
  cond_dim: 128
  vocab_size: 27
  length: ${data.k}
  n_blocks: 12
  n_heads: 12
  dropout: 0.1
